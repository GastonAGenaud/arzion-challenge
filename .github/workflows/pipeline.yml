on: [push]

jobs:
  Test:
    runs-on: cypress/browsers
    name: Test Plan
    steps:
      - name: Install
        uses: actions/checkout@v3
      - name: Test execution Admin Suite
        run: npm i && npm run test